/**
 * Native (iOS/Android): Stripe React Native SDK.
 * Metro resolves this file only for native bundles — web never imports it.
 */
export { StripeProvider, usePaymentSheet } from "@stripe/stripe-react-native";
