/** @type {const} */
const themeColors = {
  primary: { light: '#DC2626', dark: '#DC2626' }, // Red for warnings/flags
  background: { light: '#0F0F0F', dark: '#0F0F0F' }, // Near black
  surface: { light: '#1A1A1A', dark: '#1A1A1A' }, // Dark gray
  foreground: { light: '#F5F5F5', dark: '#F5F5F5' }, // Off-white text
  muted: { light: '#9CA3AF', dark: '#9CA3AF' }, // Medium gray
  border: { light: '#2D2D2D', dark: '#2D2D2D' }, // Dark borders
  success: { light: '#10B981', dark: '#10B981' }, // Green
  warning: { light: '#F59E0B', dark: '#F59E0B' }, // Amber
  error: { light: '#DC2626', dark: '#DC2626' }, // Red
  accent: { light: '#DC2626', dark: '#DC2626' }, // Red accent
  tint: { light: '#DC2626', dark: '#DC2626' }, // Red tint
};

module.exports = { themeColors };
